version: 1.0
linter: registry.jetbrains.team/p/sa/containers/qodana-js:latest
bootstrap: |
  yarn install
  npm run test-ci
  mkdir -p /data/coverage
  mv coverage/lcov.info /data/coverage
  rm -rf coverage
